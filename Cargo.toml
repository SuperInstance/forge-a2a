[package]
name = "forge-a2a"
version = "0.1.0"
edition = "2021"
description = "A2A (agent-to-agent) messaging for ForgeFlux tile pipelines"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
